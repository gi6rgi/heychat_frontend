# Copy to .env. REST + WebSocket origins of the backend.
# Leave blank to use the Vite dev proxy (see vite.config.ts).
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000

# Supabase project (Settings → API). Required for auth.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
